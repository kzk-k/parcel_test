{
  "name": "parcel_test",
  "scripts": {
    "dev": "npx parcel src/index.html --open --no-source-maps",
    "build": "rm dist/*; npx parcel build src/index.html --no-source-maps"
  },
  "devDependencies": {
    "autoprefixer": "^9.7.4",
    "parcel": "^1.12.4",
    "sass": "^1.25.0"
  }
}
